# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build output. src-tauri/target is the Rust build tree and reaches several GB;
# dist_installer is where installer.iss drops FlipperClipper-Setup.exe.
src-tauri/target/
dist_installer/

# Written by scripts/build_info.mjs on every build. It is generated from the
# commit it is built at, so a committed copy would be wrong the moment anyone
# pulled it.
src/generated/

*.tmp
